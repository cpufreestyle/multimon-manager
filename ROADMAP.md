# multimon-manager · 产品路线图 & 功能规格（v0.3 规划）

> 范围：基于当前能力（每屏壁纸、窗口跨屏/吸附、上中下/左右模板、全局快捷键 ⌘⌥、显示器热插拔监听、`profiles`/`layouts`/`settings`），规划下一阶段功能与待修技术债。
> 平台：Windows + macOS，纯 Python 标准库 + `ctypes` + `tkinter`，**零第三方依赖**（硬约束）。
> 文档状态：草案 v1 · 已启动实现（2026-09-14）
> 进度：**规划全部落地**——M1（F1 / F6 / F7 / F8 / A 债 / B 债）、M2（C 债 / F2 / F3 / F4）、M3（F5）均已实现。B 债与新增 UI 待真机验证。

---

## 1. 背景与现状

**定位**：DisplayFusion 风格的轻量多屏管理器，单文件可运行、零安装。

**已具备**
- 每屏独立壁纸（`set_wallpaper` 平台实现）
- 窗口跨屏 / 吸附 / 上中下·左右模板
- 全局快捷键（macOS ⌘⌥ + 数字键；Windows 修饰键）
- 系统托盘 + 单页滚动 UI
- 显示器热插拔监听 + 主线程 runloop 模型
- 配置持久化（`settings.json` / `profiles.json` / `layouts.json`）

**本次规划目标**：从"能控制窗口/壁纸"升级为"记住你的多屏工作流并自动适配场景"。

---

## 2. 优先级（MoSCoW）

| # | 功能 | 价值 | 可行性 | 优先级 |
|---|------|------|--------|--------|
| F6 | 显示器别名 | 中 | 高（低） | **Done（已实现 2026-09-14）** |
| F1 | 窗口布局快照 | 高 | 高（低–中） | **Done（代码早已存在）** |
| F7 | 撤销上次窗口移动 | 中 | 高（低） | **Done（已实现 2026-09-14）** |
| F8 | 设置导入 / 导出 | 中 | 高（低） | **Done（已实现 2026-09-14）** |
| A | 快捷键事件透传修复 | 高 | 高（低） | **Done（债，已修 2026-09-14）** |
| B | 全角引号几何解析修复 | 高 | 高（低） | **Done（债，已稳健化，待真机验证）** |
| C | system_profiler 超时降级 | 中 | 中 | **Done（债，已修 2026-09-14）** |
| F2 | 情景模式自动切换 | 高 | 中 | **Done（已实现 2026-09-14）** |
| F3 | 分屏吸附预览框 | 中 | 中 | **Done（已实现 2026-09-14，待真机验证）** |
| F4 | 菜单栏常驻入口(macOS) | 中 | 中 | **Done（已实现 2026-09-14）** |
| F5 | 窗口规则引擎 | 高 | 低（中–高） | **Done（已实现 2026-09-14，基础表单版，待真机验证）** |

> 排序逻辑：先交付低风险的"体验地基"（别名/快照/撤销/导入导出 + 债修复），再做需要新架构的"智能场景"（情景/预览/菜单栏），规则引擎体量最大放最后。

---

## 3. 路线图（里程碑）

### M1 — 稳定性 & 快捷增强（基线）
> 目标：补齐已知缺陷，解锁"保存/恢复我的布局"这一最高频诉求。
- [x] A 全局快捷键事件拦截（不再透传前台 App）— 已修（hotkeys_mac 匹配后 return None 消费事件）
- [x] F6 显示器别名 — 已实现（settings 存 monitor_aliases + UI 行内编辑 + 各列表/布局图显示别名）
- [x] F7 撤销上次窗口移动 — 已实现（windows/windows_mac 撤销栈 + UI 按钮 + ⌘⌥/Ctrl+Alt+Z 快捷键）
- [x] F8 设置导入/导出 — 已实现（settings.export_all/import_all 打包 zip + 自动备份）
- [x] F1 窗口布局快照 — 确认代码早已存在（layouts.py + ui._build_layouts），无需新建
- [x] B 全角引号窗口几何解析健壮化 — 已做代码层修复：`get_window_rect` 真正优先 CoreGraphics 枚举（owner+name 匹配），绕开 AppleScript 引号解析失败；待真机验证

### M2 — 智能场景
> 目标：从"手动触发"走向"自动适配"。
- [x] C system_profiler 超时降级 — 已修（超时 15s→5s + 缓存上次成功结果兜底；返回深拷贝防污染）
- [x] F2 情景模式 — 已实现（新增 `scenarios.py`：组合签名 → 绑定壁纸方案 + 布局；显示器变化时自动套用 + 情景分区 UI）
- [x] F3 分屏吸附预览框 — 已实现（新增 `snap_preview.py` 浮层 + `dragsnap_mac.py` 独立鼠标事件 tap；拖到边缘高亮、松手吸附；Windows 占位）
- [x] F4 菜单栏常驻入口 — 已实现（复用既有独立托盘子进程面板 `_tray_panel.py`，补上"切换情景"；主进程经 `cmd_channel` 新增 `apply_scenario:<签名>` 命令套用）

### M3 — 自动化（可选）
> 目标：规则驱动，零手动。
- [x] F5 窗口规则引擎 — 已实现（新增 `rules.py`：规则 CRUD / 匹配 / 区域计算 / 应用；`ui.py` 新增"窗口规则"分区：规则列表 + 表单 + 立即应用 + 新窗口自动应用轮询。UI 走"基础表单 + JSON 存储"路线）

---

## 4. 功能规格（Feature Specs）

### F1 · 窗口布局快照
**目标**：记录并一键还原"当前所有可见窗口的真实位置/尺寸"。
**用户故事**：作为多屏用户，我希望拔掉外接屏或窗口被打乱后，能一键恢复到我摆好的样子，以免每次手动重排。
**需求**
- 捕获当前所有可见窗口的 `(owner, title, monitor, x, y, w, h)`。
- 支持命名快照、列表管理（增/删/重命名/恢复）。
- 恢复时按 `owner+title` 匹配当前窗口；匹配不上的窗口记录日志不报错。
- 快照存 `layouts.json`（已存在该文件，复用结构）。
**验收标准**
- 保存 10 个窗口布局，恢复后位置误差 < 2px（同分辨率下）。
- 显示器数量变化（拔屏）时恢复不崩溃，缺失目标屏的窗口落到主屏。
**范围/非范围**：含位置+尺寸+所在屏；**不含**窗口层级/Z-order、最小化状态还原。
**依赖**：已有 `get_window_list` / `get_window_rect` / `set_window_rect`。
**估计**：M/M（2–3 天）。

### F2 · 情景模式自动切换
**目标**：识别出"当前接的是哪组显示器"，自动套用对应的壁纸+窗口布局预设。
**用户故事**：作为通勤办公用户，我接上办公室双屏时希望自动进入"办公布局"，回到家里单屏时自动回到"影音布局"。
**需求**
- 用已有显示器指纹（EDID/UUID 子集）生成"显示器组合签名"。
- 用户可将某一快照/壁纸组合绑定到某个签名，命名为情景。
- 显示器监听触发（已有主线程轮询）后，匹配签名并静默应用；无匹配则不动。
**验收标准**
- 拔/插显示器后 3 秒内自动应用匹配情景；无匹配时不打扰。
- 提供"绑定当前为情景 X"的一键操作。
**范围/非范围**：仅套用既有预设；**不**实时学习用户习惯。
**依赖**：F1（快照）、显示器指纹监听。
**估计**：M（3–4 天）。

### F3 · 分屏吸附预览框
**目标**：拖拽/吸附过程中，半透明高亮窗口将落入的区域。
**用户故事**：作为用户，我希望在松手前就知道窗口会吸附到哪块区域，避免来回微调。
**需求**
- 在吸附判定命中时，绘制一个置顶、半透明、无交互的 overlay 窗口标出目标区域。
- 拖拽结束（mouseup / drag exit）即销毁 overlay。
- 仅 macOS 需要自建 overlay（`NSPanel`）；Windows 可用 `DrawFocusRect`/红框。
**验收标准**
- 预览在拖拽过程中实时跟随，松手后窗口精确落入预览区域。
- overlay 不拦截鼠标事件、不进入窗口列表。
**范围/非范围**：仅吸附场景（左右/上中下）；**不含**自由缩放预览。
**依赖**：已有 snap 逻辑；需主线程绘制（符合既有的主线程 runloop 约束）。
**估计**：M（2–3 天，macOS 优先）。

### F4 · 菜单栏常驻入口（macOS）
**目标**：在 macOS 菜单栏提供常驻图标，随时触发布局/情景。
**用户故事**：作为 macOS 用户，我希望不打开主窗口也能快速切换布局，尤其当全局快捷键未授权时。
**需求**
- 使用 `rumps` 风格需第三方 → **违反零依赖**；改用 `pyobjc`？同样违规。
- **合规方案**：用 `tkinter` 自建 minimal 菜单栏 item 受限；可依赖系统 `NSStatus` 通过 `ctypes` 调用（保持零依赖）。
- 菜单项：应用布局快照 / 切换情景 / 打开主窗口 / 退出。
**验收标准**
- 菜单栏图标常驻，主窗口关闭后仍可触发布局。
- 不影响现有 Dock 行为。
**范围/非范围**：macOS only；Windows 维持系统托盘。
**依赖**：`ctypes` 调用 `NSStatus`（已用在其它 macOS 模块，可复用模式）。
**估计**：M（2–3 天）。

### F5 · 窗口规则引擎
**目标**：按 App 名/窗口标题，自动把窗口分配/钉到指定屏或位置。
**用户故事**：作为重度多屏用户，我希望"浏览器永远在左屏、终端永远在右屏"，无需每次手动拖。
**需求**
- 规则：`{match: owner/title 正则, action: monitor=N | position=预设 | pin}`，可启停、排序（先匹配先执行）。
- 触发：窗口创建/焦点变化/手动"应用规则"按钮。
- 规则存 `settings.json`。
**验收标准**
- 配置 3 条规则，新开匹配窗口自动落到目标屏（误差 < 2px）。
- 规则冲突时按用户定义的优先级，不循环移动。
**范围/非范围**：不含基于时间的规则；不含窗口尺寸自动调整（除非规则显式指定）。
**依赖**：F1 的窗口枚举、移动能力。
**估计**：L（5–7 天，含 UI 编辑器）。

### F6 · 显示器别名 ★低风险高收益
**目标**：用户自定义屏名，UI 下拉显示别名而非 `Display 1/2`。
**用户故事**：作为用户，我希望在配置里看到"办公室左屏"而不是冷冰冰的 Display 2。
**需求**
- 设置中为每个显示器指纹配置别名；下拉/列表优先显示别名，无别名回退默认名。
- 别名存 `settings.json`，与显示器指纹绑定（避免按序号错配）。
**验收标准**
- 配置别名后，所有 UI 文本显示别名；删除别名回退默认。
- 重连同一显示器后别名保持。
**范围/非范围**：仅展示层；不改底层几何逻辑。
**依赖**：显示器指纹（已有）。另：别名可**顺便缓解** C 的屏名退化——退化时仍用别名显示。
**估计**：S（0.5–1 天）。

### F7 · 撤销上次窗口移动
**目标**：误移动/吸附窗口后，一步回退到移动前状态。
**用户故事**：作为用户，我手滑把窗口吸错了位置，希望 Cmd+Z 类操作能还原。
**用户故事（续）**：我希望有统一入口回退最近一次窗口操作。
**需求**
- 在执行移动/吸附前，压栈记录受影响的窗口原几何；提供"撤销"按钮 + 可选快捷键。
- 仅保留最近 N 步（默认 10），不跨会话。
**验收标准**
- 误移后撤销，窗口精确回到原位（误差 < 2px）。
- 栈空时撤销按钮禁用。
**范围/非范围**：仅窗口移动类操作；不含壁纸/设置变更。
**依赖**：已有移动入口（统一经一处 `move_window` 包装即可埋点）。
**估计**：S（0.5–1 天）。

### F8 · 设置导入 / 导出
**目标**：一键备份或迁移 `settings.json` / `profiles.json` / `layouts.json`。
**用户故事**：作为用户，我希望换机或重装后能快速恢复全部配置。
**需求**
- 导出：打包三文件为单个 `.json` 或 `.zip`（标准库 `zipfile`，零依赖）。
- 导入：校验结构后覆盖；导入前自动备份当前配置。
**验收标准**
- 导出文件在另一台机器导入后，布局/别名/快捷键一致生效。
- 损坏文件导入失败且原配置不被破坏。
**范围/非范围**：不含壁纸图片迁移（仅路径引用）。
**依赖**：仅文件 I/O。
**估计**：S（0.5–1 天）。

---

## 5. 待修复技术债

| ID | 问题 | 影响 | 修复方向 | 优先级 |
|----|------|------|----------|--------|
| **A** | 全局快捷键触发后事件透传前台 App（⌘⌥+1 同时被前台程序收到） | 快捷键"双重触发"，体验割裂 | 在 `CGEventTap` 回调中对已处理事件消费（`CGEventTapEnable`/返回 `None`）；Windows 用 `return 1` 阻止传播 | **Must** |
| **B** | 窗口标题含全角引号（OneNote / 微信开发者工具）时几何读取失败，靠重试兜住 | 偶发窗口无法移动/吸附 | 解析 `System Events`/`AX` 返回时去除外层引号污染，使用 `value of attribute "AXPosition"` 直读 | **Must** |
| **C** | `system_profiler` 偶发 15s 超时致屏名退化 | UI 显示 `Display N` 而非真实名 | 设超时 + 缓存上次成功结果；配合 F6 别名兜底 | **Should** |

---

## 6. 风险与依赖

- **零依赖硬约束**：F4 菜单栏若需 `rumps`/`pyobjc` 即违规，必须 `ctypes` 实现（已评估可行但工作量上升）。
- **主线程 runloop 约束**：F3 预览、F4 菜单栏必须在主线程绘制，沿用既有模型，不得起后台 `CFRunLoopRun()`。
- **macOS 辅助功能授权**：F1/F2/F5 的窗口控制首次需授权，安装流程已有引导，需在新功能入口复用。
- **跨平台差异**：F3/F4 的 UI 表现 macOS/Windows 实现路径不同，需双平台验证。
- **依赖链**：F2 依赖 F1；F5 依赖 F1；C 部分被 F6 缓解。

---

## 7. 成功指标

- M1 后：布局快照日均使用 ≥ 1 次/活跃用户；快捷键透传客诉归零。
- M2 后：情景模式自动命中率 ≥ 90%（有匹配签名时）；吸附预览使"微调重拖"下降。
- M3 后：规则引擎覆盖 ≥ 60% 用户的常驻窗口分配。

---

## 8. 假设与开放问题

1. **"全部"的实现节奏**：本文档把 8 功能 + 3 债全部纳入规划。落地时是否严格按 M1→M3 顺序？还是先挑 1–2 个做深？
2. **F4 菜单栏**：是否接受 `ctypes` 实现的额外工作量（保持零依赖）？还是放宽允许 `pyobjc`？
3. **F5 规则引擎 UI**：是否需要可视化规则编辑器，还是先用 JSON 配置 + 基础表单？
   → **已决策（2026-09-14）**：采用「基础表单 + JSON 存储」落地；可视化编辑器（拖拽 / 多条件组合）留待后续迭代。
4. **快照匹配键**：仅用 `owner+title` 是否足够？是否需要加入窗口尺寸相似度以提高恢复鲁棒性？

> 下一步建议：评审本路线图 → 确认 M1 范围 → 进入实现（从 F6/F7/F8 低风险项热身，再攻 F1 + A/B 债）。
